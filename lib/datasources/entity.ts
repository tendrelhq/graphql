import type { Request } from "express";
import type { Fragment } from "postgres";
import { match } from "ts-pattern";
import { sql } from "./postgres";

export function makeEntityLoader(req: Request) {
  //
}
