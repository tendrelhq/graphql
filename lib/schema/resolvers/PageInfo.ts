import type { PageInfoResolvers } from "@/schema";

// Note that we should never reach this resolver. We should resolve it at the
// connection level.
export const PageInfo: PageInfoResolvers = {
  /* Implement PageInfo resolver logic here */
};
