{
  "name": "@tendrelhq/graphql",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "clean": "git clean -dfx -e .direnv -e .env.local -e .envrc -e .pre-commit-config.yaml -e /data -e postgrestools.jsonc",
    "server:build": "bun run --filter ./packages/server build",
    "server:generate": "bun run --filter ./packages/server generate"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@faker-js/faker": "^9.7.0",
    "@vtsls/language-server": "^0.2.8",
    "graphql-language-service-cli": "^3.5.0",
    "prettier": "^3.5.3",
    "typescript-language-server": "^4.3.4"
  },
  "patchedDependencies": {
    "@ardatan/relay-compiler@12.0.0": "patches/@ardatan%2Frelay-compiler@12.0.0.patch"
  }
}
